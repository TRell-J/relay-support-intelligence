import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev badge sits on top of the Reset demo control.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
